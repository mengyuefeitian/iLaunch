## 1.8.1 - 2026-08-13

### 修复
- 搜索框输入的第一个字母不生效（AppKit `interpretKeyEvents` 之前调用在 `NSTextField` 本身上，而实际负责插入文字的是它的 field editor）
- 点击搜索结果列表中的项目会退出悬浮窗但不打开对应 App（搜索状态下的 AppKit 点击监听器把所有点击都当作空白区域退出处理，没有判断点击是否落在结果图标上）

## 1.8.0 - 2026-07-30

### 新功能
- 产品更名：**InceptLaunch → iLaunch**（应用标识、设置界面、打包 `.app`/DMG、Bundle ID `com.ilaunch.iLaunch`、README 与多语言文案统一）
- 首次启动时将 `Application Support/InceptLaunch/` 下的已有数据迁移到 `iLaunch/`

## 1.7.22 - 2026-07-30

### 修复
- DMG 安装界面：圆弧箭头与箭头方向对齐，Applications 拖放目标图标通过 Finder 替身恢复，`.background` 文件夹隐藏并移出可视区域

## 1.7.21 - 2026-07-30

### 新功能
- 文件夹弹层从网格瓦片位置放大打开，关闭时缩回原位（过渡中保持 3×3 小图标形态）

### 修复
- 打开大屏后的第一次点击（启动 app / 打开文件夹 / 点空白退出）不再失效
- 打开/关闭文件夹时不再卡死或卡在半开半关状态
- 上一次关闭动画的定时器不会误关新打开的文件夹
- 关闭大屏不再使用 `NSApp.hide`，改为把焦点还给之前的前台应用

## 1.7.13 - 2026-07-30

### 新功能
- 放大后的 2×2 大文件夹：可不打开弹层，直接点击内部迷你图标启动对应应用

### 修复
- 点击应用后不再卡顿 2–5 秒才退大屏 — 先关闭全屏叠加层，再异步启动应用

## 1.6.19 - 2026-07-26

### 新功能
- 文件夹弹窗使用模糊壁纸背景（Liquid Glass 质感）
- 拖入文件夹接受阈值时文件夹瓦片放大反馈
- 从文件夹拖出到网格时实时留缝与新建文件夹感应
- 设置页支持网格行列、图标大小（S/M/L）、显示/隐藏应用名称
- 网格行列数随用户偏好动态生效
- 新增俄语本地化及布局/图标相关偏好键

### 修复
- 拖拽翻页交接浮动轨迹、文件夹交接幽灵层与放大行溢出
- 恢复文件夹弹窗内部重排与拖出
- 搜索区与放大文件夹毛玻璃风格与小文件夹一致
- 搜索毛玻璃、可编辑输入、网格间距与放大文件夹边界

## 1.5.5 - 2026-07-25

### 新功能
- 设置页面重构为侧边栏导航,新增"关于"页面
- 搜索结果中为隐藏应用添加眼睛徽章图标
- 支持在文件夹弹窗内拖拽重排应用
- 主网格实时重排:浮动覆盖层、瓦片位移与隐入动画
- 网格瓦片位置变化添加隐式弹簧动画
- 新增日语/韩语国际化,支持运行时切换语言并改进日志输出

### 修复
- 菜单栏图标同步,本地化搜索/背景标签,居中占位符
- 缓存背景图片,避免拖拽重排时闪烁
- 防止 drop 时重复移动,并持久化文件夹重排结果
- 修复 `computeGridTargetIndex` 中的变量遮蔽问题
- 修复系统应用切换与文件夹拖出阈值

### 重构
- 使用基于 ID 的 `ForEach` 身份标识以支持网格动画
